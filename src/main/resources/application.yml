spring:
  datasource:
    url: jdbc:sqlserver://localhost;databaseName=hibernate;integratedSecurity=true;
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
